version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build Starting on `date`
      - cd DeployServer
      - chmod +x ./gradlew
      - ./gradlew build
  post_build:
    commands:
      - echo $(basename ./seb40_main_015/backend/build/libs/*.jar)
artifacts:
  files:
    - seb40_main_015/backend/build/libs/*.jar
    - seb40_main_015/backend/scripts/**
    - seb40_main_015/backend/appspec.yml
  discard-paths: yes
